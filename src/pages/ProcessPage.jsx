import { PlaceholderPage } from './PlaceholderPage'

export function ProcessPage() {
  return (
    <PlaceholderPage
      title="Process"
      description="Discover → Plan → Design → Develop → Test → Launch → Improve."
    />
  )
}
