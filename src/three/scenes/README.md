# Hero atmosphere scene

Implement `HeroAtmosphereScene.js` only during the **HomeHero** build phase after foundation approval.

Until then, `SceneCanvas` remains available as infrastructure without a mounted marketing scene.
